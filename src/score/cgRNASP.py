import subprocess
import os

def cgRNASP(pdb_dir, out_dir):
    res = subprocess.run(f"./third_party/cgRNASP/cgRNASP {pdb_dir} {energy_file]}", shell = True, capture_output=True)
    return 

def cgRNASP_P(pdb_dir, out_dir):
    res = subprocess.run(f"./third_party/cgRNASP-P/cgRNASP-P {pdb_dir} {energy_file]}", shell = True, capture_output=True)

    return

def cgRNASP_PC(pdb_dir, out_dir):
    res = subprocess.run(f"./third_party/cgRNASP-P/cgRNASP-P {pdb_dir} {energy_file]}", shell = True, capture_output=True)

    return 

